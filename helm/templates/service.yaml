---
apiVersion: v1
kind: Service
metadata:
  name: benz-walter-crls
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: benz-walter-crls
